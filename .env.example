# Stripe secret key, used server-side only (src/app/api/checkout/route.ts)
# to create Checkout Sessions. Never exposed to the client, never committed —
# copy this file to .env.local and fill in the real value.
STRIPE_SECRET_KEY=
